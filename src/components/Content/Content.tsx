import React from "react";
import CommitHistory from "../../pages/github-commit-viewer";

const Content: React.FC = () => {
    return(
        <div>
            <CommitHistory/>
        </div>
    )
};

export default Content;